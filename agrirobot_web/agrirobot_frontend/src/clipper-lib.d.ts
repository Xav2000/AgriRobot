// Déclaration de types pour clipper-lib (port JavaScript de Clipper).
// Le paquet n'embarque pas de types ; déclaration volontairement permissive
// (any) — seules les APIs utilisées par src/lib/worklines.ts le sont :
// ClipperOffset (offsetting), Clipper (différence booléenne), Paths.
declare module 'clipper-lib';
